<!DOCTYPE html>
<html style="height: 100%; width: 100%">
  <head>
    <title>FlightBooker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body style="height: 97%; width: 100%">
      <nav style="position: fixed; width: 100%; z-index: 2; top: 0; height: 3.5em; padding: 0.5em;" class="bg-white"><%= render partial: 'layouts/nav' %></nav>
     <p class="notice"><%= notice %></p>
     <p class="alert"><%= alert %></p>
    <main style="padding: 0.25em;" class="d-flex flex-column align-items-center">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </main>
  </body>
</html>
